name: Build Geranium IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0

      - name: Init submodules from upstream
        run: |
          git submodule init
          git submodule sync
          git submodule update --init --recursive --remote

      - name: Select Xcode
        run: |
          sudo xcode-select -switch /Applications/Xcode_16.2.app
          xcodebuild -version

      - name: Install Theos
        run: |
          brew install ldid xz
          git clone --recursive https://github.com/theos/theos.git ~/theos
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV

      - name: Install iPhoneOS 15.6 SDK
        run: |
          git clone --depth=1 --filter=blob:none --sparse https://github.com/theos/sdks.git ~/theos-sdks
          cd ~/theos-sdks
          git sparse-checkout set iPhoneOS15.6.sdk
          cp -r iPhoneOS15.6.sdk $THEOS/sdks/
          echo "✅ SDK installed: $(ls $THEOS/sdks/)"

      - name: Patch RootHelper SDK version
        run: |
          sed -i '' 's/iPhoneOS14.5.sdk/iPhoneOS15.6.sdk/g' RootHelper/Makefile
          echo "✅ Patched: $(grep 'SDK' RootHelper/Makefile)"

      - name: Build RootHelper
        run: |
          cd RootHelper
          make package FINALPACKAGE=1
        env:
          THEOS: ${{ env.THEOS }}

      - name: Build Geranium with Xcode
        run: |
          xcodebuild \
            -project Geranium.xcodeproj \
            -scheme Geranium \
            -configuration Release \
            -sdk iphoneos \
            -destination "generic/platform=iOS" \
            -derivedDataPath build/DerivedData \
            SDKROOT=$THEOS/sdks/iPhoneOS15.6.sdk \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO

      - name: Run ipabuild.sh
        run: |
          chmod +x ipabuild.sh
          bash ipabuild.sh

      - name: Rename tipa to ipa
        run: |
          find build/ -name "*.tipa" -exec bash -c 'cp "$1" "${1%.tipa}.ipa"' _ {} \;

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: Geranium-IPA
          path: |
            build/*.ipa
            build/*.tipa
          if-no-files-found: error
          retention-days: 30